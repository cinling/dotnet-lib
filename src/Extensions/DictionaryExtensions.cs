using System.Collections;
using System.Collections.Generic;

namespace Cinling.Lib.Extensions {
    
    public static class DictionaryExtensions {

        // /// <summary>
        // /// 字典键值翻转
        // /// </summary>
        // /// <param name="originDict"></param>
        // /// <returns></returns>
        // public static IDictionary<string, string> Flip(this IDictionary<string, string> originDict) {
        //     var newDict = originDict.GetType().CreateInstance<IDictionary>();
        //     foreach (var (key, value) in originDict) {
        //         newDict[value] = key;
        //     }
        //     return (IDictionary<string, string>) newDict;
        // }
    }
}